export interface summaryTypes {
    id:string;
    summary:string;
    flags:string[];
}

export interface NavTypes {
  logo: string;
  loginText?: string;
  signupText?: string;
  authText?: string;
}

